name: Go Test CI

on:
  push:
    branches: [main, clean-architecture-refactor]  # You can list branches
  pull_request:
    branches: [main]

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: ⬇️ Checkout code
        uses: actions/checkout@v3

      - name: 🧰 Set up Go
        uses: actions/setup-go@v4
        with:
          go-version: '1.20'

      - name: 📦 Install dependencies
        run: go mod tidy

      - name: 🧪 Run unit tests
        run: go test ./... -v -coverprofile=coverage.out

      - name: 📊 Upload coverage report (optional)
        uses: actions/upload-artifact@v3
        with:
          name: code-coverage
          path: coverage.out
